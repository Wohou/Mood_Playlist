"use client"

interface AddSectionButtonProps {
  onAddSection: (title: string) => void
}

// This component has been disabled as the section feature is no longer supported
export default function AddSectionButton({ onAddSection }: AddSectionButtonProps) {
  // Return null to render nothing
  return null
}
